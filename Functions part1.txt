// Methods (Code Reusabilty)
function sayMyname(){

console.log("Hitesh")
// when we define function they are called Parameters and 
   when we call the function they are called arguments
}
function addTwoNumbers(number1,number2){
	number1+number2
	}
	addTwoNumbers(3,4)


function addTwoNumbers(number1,number1){
	let result = number1+number2
	return result
	}
	const result = addTwoNumbers(3,5);
	console.log("Result: " result);
	
	function loginUserMessage(username){
	return `${username} just logged in`
	}
//	console.log(loginUserMessage("hitesh"))
	
	


// Functions part 2

function calculateCartPrice(...num1){
  return num1;
}

console.log(calculateCartPrice(2));
 
// Handling Objects in the function
const user = {
	username: "Hitesh",
	price: 199
}

function handleObject(anyobject){
	console.log(`username is ${anyobject} and price is ${anyobject.prices}`)

}
handleObject(user);

handleObject({
	username:"sam",
	price:399

})
const mynewArray = [200,400,100,600]

funcction returnSecondvalue(getarray){
return getarray[1];
}

console.log(returnSecondvalue(mynewArray));







